quarkus.mongodb.connection-string=${MONGODB_CONNECTION_STRING:mongodb://localhost:27018}
# Ensure write concern does not require journaling if not necessary
quarkus.mongodb.write-concern = ACKNOWLEDGED

quarkus.log.category."org.mongodb.driver".level=DEBUG
quarkus.log.console.enable=true